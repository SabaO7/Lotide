// index.js
const head   = require('./head');
const tail   = require('./tail');
const middle = require('./middle');

module.exports = {
  head,
  tail,
  middle
};

//add all the rest of the functions i.e.e map, letterposition etc. except the assert stuff 

//.1.0.0.1 change the format in teh package.json
